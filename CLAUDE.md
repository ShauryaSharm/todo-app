# To Do — project instructions

**The living reference for this project is [`Forever Instructions.md`](Forever%20Instructions.md)
in this same folder. Read it now, before doing anything else in this repo.**

That file holds the architecture, endpoints, env vars, features, deploy workflow, every
gotcha, and the current status. Keep it current — update it in the same commit as any
change. This file exists only because `CLAUDE.md` is the filename that auto-loads into
every session; the content deliberately lives under the name Shaurya chose.
